extends baseEmail

block content
  //- p Hola #{firstName},
  p Correo enviado por formulario de contacto
  p Nombre: #{name}
  p Email: #{message.email}
  p Asunto: #{message.subject}
  p Mensaje: #{message.message}
  a(href=`${url}`, target='_blank') #{url}
  table.btn.btn-primary(role='presentation', border='0', cellpadding='0', cellspacing='0')
    tbody
      tr
        td(align='left')
          table(role='presentation', border='0', cellpadding='0', cellspacing='0')
            tbody
              tr
                td
                  a(href=`${url}`, target='_blank') EmotioKanis9
  //- p Si no olvidó su contraseña, ignore este correo.
